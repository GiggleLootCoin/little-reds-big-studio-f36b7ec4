import { mkdir, writeFile } from "node:fs/promises";

await mkdir("dist/server", { recursive: true });
await writeFile("dist/server/worker-wrapper.js", `import studioServer from "./server.js";
import { handleProductionQwenVoiceClone } from "../../src/lib/qwen-production-gateway-v2.ts";
const LANGUAGE_CODES={english:"en",french:"fr",german:"de",spanish:"es",italian:"it",portuguese:"pt",dutch:"nl",japanese:"ja",korean:"ko",chinese:"zh",mandarin:"zh",russian:"ru",arabic:"ar",hindi:"hi"};
const PRESET_SPEAKERS={Ryan:"angus",Aiden:"orion",Vivian:"asteria",Serena:"luna",Uncle_Fu:"zeus",Dylan:"perseus",Eric:"helios",Ono_Anna:"stella",Sohee:"athena"};
function lang(v){const x=String(v||"").trim().toLowerCase();return !x||x==="auto"?undefined:LANGUAGE_CODES[x]||x}
function text(r){if(typeof r==="string")return r.trim();if(!r||typeof r!=="object"||r instanceof Response||Array.isArray(r))return"";for(const k of ["response","text","generated_text","output","content"])if(typeof r[k]==="string"&&r[k].trim())return r[k].trim();const m=Array.isArray(r.choices)?r.choices[0]?.message:null;return m&&typeof m.content==="string"?m.content.trim():""}
function err(m,s=500){return Response.json({ok:false,error:m},{status:s,headers:{"cache-control":"no-store"}})}
async function stt(request,env){if(!env.AI)return err("Cloudflare Workers AI binding is not configured.",503);let b;try{b=await request.json()}catch{return err("Invalid speech request.",400)}const audio=String(b?.audioBase64||"").trim();if(!audio)return err("Audio is required for speech recognition.",400);const l=lang(b?.language);for(const model of ["@cf/openai/whisper-large-v3-turbo","@cf/openai/whisper"]){try{const r=await env.AI.run(model,model.endsWith("whisper")?audio:{audio,task:"transcribe",...(l?{language:l}:{}),vad_filter:false});const t=text(r);if(t)return Response.json({text:t,transcription:t})}catch{}}return err("Speech recognition could not produce a result. Please try speaking for a little longer.",503)}
async function chat(request,env){if(!env.AI)return err("Cloudflare Workers AI binding is not configured.",503);let b;try{b=await request.clone().json()}catch{return err("Invalid Buddy chat request.",400)}const messages=Array.isArray(b?.messages)&&b.messages.length?b.messages:[{role:"user",content:String(b?.prompt||b?.text||"").trim()}];try{const r=await env.AI.run("@cf/qwen/qwen3.8-27b",{messages,max_tokens:320,temperature:.55,top_p:.9});const t=text(r);if(t)return Response.json({text:t},{headers:{"cache-control":"no-store","x-buddy-model":"qwen3.8-27b"}})}catch(e){console.error("Buddy chat failed",e)}return studioServer.fetch(request,env,undefined)}
async function tts(request,env){if(!env.AI)return err("Cloudflare Workers AI binding is not configured.",503);let b;try{b=await request.json()}catch{return err("Invalid voice request.",400)}const t=String(b?.text||b?.target_text||b?.prompt||"").trim().slice(0,1200);if(!t)return err("Voice text is empty.",400);const requested=String(b?.speaker||"Ryan").trim(),speaker=PRESET_SPEAKERS[requested];if(!speaker)return err("Unsupported Buddy preset voice: "+requested,400);if((lang(b?.language)||"en")!=="en")return err("Buddy preset voices currently require English.",400);try{const r=await env.AI.run("@cf/deepgram/aura-1",{text:t,speaker,encoding:"mp3"},{returnRawResponse:true});if(!(r instanceof Response)||!r.ok)throw new Error("Aura-1 returned "+(r?.status||"no response"));const h=new Headers(r.headers);h.set("content-type",h.get("content-type")||"audio/mpeg");h.set("cache-control","no-store");h.set("x-buddy-voice",requested);return new Response(r.body,{status:r.status,headers:h})}catch(e){console.error("Buddy preset TTS failed",e);return err("Buddy preset voice generation failed. "+(e instanceof Error?e.message:String(e)),502)}}
function health(env){return Response.json({ok:true,capability:"voice-clone",backend:"Qwen3-TTS Gradio gateway",models:["0.6B","1.7B"],qwenSpaceConfigured:Boolean(env?.QWEN_TTS_SPACE_URL)})}
export default{async fetch(request,env,ctx){const path=new URL(request.url).pathname.replace(/\/$/,"")||"/";if (path === "/api/ai/voice-clone" && request.method === "GET")return health(env);if ((path === "/api/ai/voice-clone" || path === "/api/voice-clone") && request.method === "POST"){const r=path==="/api/ai/voice-clone"?new Request(new URL("/api/voice-clone",request.url),request):request;return handleProductionQwenVoiceClone(r,env)}if (path === "/api/ai/tts" && request.method === "POST")return tts(request,env);if (path === "/api/ai/speech-to-text" && request.method === "POST")return stt(request,env);if (path === "/api/ai/chat" && request.method === "POST")return chat(request,env);if (path === "/api/ai" && request.method === "POST"){try{const b=await request.clone().json(),c=String(b?.capability||"").toLowerCase().replace(/_/g,"-");if(["voice-clone","voiceclone","clone"].includes(c))return handleProductionQwenVoiceClone(request,env);if(["chat","conversation","text-generation"].includes(c))return chat(request,env)}catch{}}return studioServer.fetch(request,env,ctx)}};
`, "utf8");