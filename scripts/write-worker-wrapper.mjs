import { mkdir, writeFile } from "node:fs/promises";

await mkdir("dist/server", { recursive: true });
// Keep the production chat contract explicit while falling back to the canonical
// server router when the Cloudflare AI allocation is exhausted.
// Contract: max_tokens: 320.
const wrapper = [
  'import studioServer from "./server.js";',
  'import { handleProductionQwenVoiceClone } from "../../src/lib/qwen-production-gateway-v2.ts";',
  'function health(env){return Response.json({ok:true,capability:"voice-clone",backend:"Qwen3-TTS Gradio gateway",models:["0.6B","1.7B"],qwenSpaceConfigured:Boolean(env?.QWEN_TTS_SPACE_URL)},{headers:{"cache-control":"no-store"}})}',
  'async function speechFallback(request,env,ctx){if(!env?.HF_TOKEN)return null;let body;try{body=await request.json()}catch{return Response.json({ok:false,error:"Invalid JSON request."},{status:400})}const audio=typeof body?.audioBase64==="string"?body.audioBase64.trim():"";if(!audio)return Response.json({ok:false,error:"Audio is required for speech recognition."},{status:400});try{const response=await fetch("https://router.huggingface.co/hf-inference/models/openai/whisper-large-v3",{method:"POST",headers:{Authorization:`Bearer ${env.HF_TOKEN}`,"Content-Type":"application/json"},body:JSON.stringify({inputs:audio,parameters:{return_timestamps:false}})});const payload=await response.json().catch(()=>null);if(response.ok&&payload&&typeof payload.text==="string"&&payload.text.trim())return Response.json({text:payload.text.trim(),transcription:payload.text.trim()},{headers:{"cache-control":"no-store","x-stt-provider":"huggingface-whisper-large-v3"}});console.warn("Hugging Face STT fallback failed",response.status,payload)}catch(error){console.warn("Hugging Face STT fallback request failed",error)}return studioServer.fetch(new Request(request.url,{method:"POST",headers:new Headers(request.headers),body:JSON.stringify(body??{})}),env,ctx)}',
  'async function appResponse(request,env,ctx){const url=new URL(request.url);const path=url.pathname.replace(/\\/$/,"")||"/";if(path.startsWith("/assets/")||path==="/favicon.ico"||path==="/manifest.webmanifest"){if(env?.ASSETS)return env.ASSETS.fetch(request);return new Response("Static assets binding is not configured.",{status:503})}const response=await studioServer.fetch(request,env,ctx);if(request.method==="GET"&&response.headers.get("content-type")?.toLowerCase().includes("text/html")){const headers=new Headers(response.headers);headers.set("cache-control","no-store, max-age=0, must-revalidate");headers.set("pragma","no-cache");return new Response(response.body,{status:response.status,statusText:response.statusText,headers})}return response}',
  'export default{async fetch(request,env,ctx){const url=new URL(request.url);const path=url.pathname.replace(/\\/$/,"")||"/";if(path === "/api/ai/chat" && request.method === "POST"){if(!env?.AI)return studioServer.fetch(request,env,ctx);let body;try{body=await request.json();const messages=Array.isArray(body?.messages)?body.messages:[{role:"user",content:String(body?.prompt||body?.text||"")}];const result=await env.AI.run("@cf/qwen/qwen3.8-27b",{messages,max_tokens: 320,temperature:0.55,stream:false});return Response.json(result,{headers:{"cache-control":"no-store","x-chat-provider":"cloudflare-qwen3.8-27b"}})}catch(error){const message=error instanceof Error?error.message:String(error);if(message.includes("4006")||message.includes("3040")||message.toLowerCase().includes("daily free allocation")||message.toLowerCase().includes("capacity")||message.toLowerCase().includes("out of capacity")){return studioServer.fetch(new Request(request.url,{method:"POST",headers:new Headers(request.headers),body:JSON.stringify(body??{})}),env,ctx)}return Response.json({ok:false,error:message||"Buddy chat engine failed."},{status:502,headers:{"cache-control":"no-store"}})}}if(path === "/api/ai/speech-to-text" && request.method === "POST")return speechFallback(request,env,ctx);if(path==="/api/ai/voice-clone"&&request.method==="GET")return health(env);if((path==="/api/ai/voice-clone"||path==="/api/voice-clone")&&request.method==="POST"){const r=path==="/api/ai/voice-clone"?new Request(new URL("/api/voice-clone",request.url),request):request;return handleProductionQwenVoiceClone(r,env)}return appResponse(request,env,ctx)}};',
].join("\n");
await writeFile("dist/server/worker-wrapper.js", wrapper, "utf8");
