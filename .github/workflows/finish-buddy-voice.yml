name: Finish Buddy Voice Routing

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-shot Buddy voice fixer disabled; production routing is maintained by the normal validation and deploy workflows."
